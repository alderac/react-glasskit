---
"react-glasskit": major
---

Finalize the React GlassKit v1 public package contract.

The v1 package now presents a compiled `dist`-only npm surface with documented
barrel and CSS entrypoints, removes source-path package exports, refreshes
install guidance for published-package usage, and softens public positioning so
GlassKit reads as a focused React workspace material layer.
